# shoes-project — Роадмап и трекер workflow

Веб-приложение-**примерка** кроссовок. Этот файл — **единый трекер прогресса**:
продукт, что сделано, что делать, решения и статусы. Обновляем по ходу.

> Статусы фаз: `⬜ не начато` · `🟡 в работе` · `✅ готово` · `⏸ потом`.

---

## Что это за продукт

**Примерка, а не подбор.** Пользователь выбирает конкретную модель и видит, как она
**сидит на его стопе** — через **рентген-оверлей**: силуэт стопы просвечивает под
контуром внутреннего пространства кроссовка, видно где тесно, где свободно.

**Поток на экране:**
1. **Рисунок (рентген-оверлей посадки)** — стопа vs внутренний контур модели; зоны
   «тесно / впору / свободно».
2. **Фидбэк** — словами: подходит / тесно / свободно + почему + уровень уверенности.

Это НЕ подбор и НЕ рекомендации («предложи мне модель» — потом). Реалистичное фото
«как смотрится на ноге» — отдельная необязательная опция на потом.

## Честное обещание

Примерка-ориентир: видишь приблизительную посадку модели на своей стопе + честную
оценку. Точного замера комфорта не обещаем — данные колодок производителей закрыты.

**Дисклеймеры (обязательны):**
- Контур кроссовка **приблизительный** (из размера + репутации ширины, не из колодок).
- Оверлей **2D** (длина × ширина): не показывает подъём/объём сверху.
- Без ориентира (A4 на фото **или** введённого размера) оценка **недостоверна** —
  говорим прямо.

---

## Зафиксированные решения

| Тема | Решение |
| --- | --- |
| Суть | Примерка: рентген-оверлей посадки → фидбэк. **Не подбор.** |
| Порядок на экране | рисунок (оверлей) → фидбэк |
| Рисунок | Рентген-оверлей: силуэт стопы vs внутренний контур модели; SVG/canvas, **без генеративного AI**, без оплаты за картинки |
| Откуда контур модели | приблизительно: длина из размера + ширина из репутации (узкая/стандартная/широкая) |
| Стек | Vite + React + Node-сервер (node:http) |
| LLM сейчас | заглушка (mock); потом Claude API + веб-поиск для атрибутов |
| Ключ API | на сервере, не во фронте |
| Фидбэк | строгие детерминированные правила; LLM только текст объяснения |
| Ввод стопы | фото с A4 **или** ручной ввод (длина/ширина/размер) с подписями против путаницы |
| Ориентир | есть A4/размер → оверлей+фидбэк; нет → предупреждение о недостоверности |
| Реалистичное фото «как выглядит» | необязательная опция, потом (генеративно, пометка «примерный вид») |
| База/каталог рекомендаций | потом |

---

## Снижение галлюцинаций (приоритет)

| Где | Метод |
| --- | --- |
| Атрибуты модели | структурированный вывод; запрет выдумывать числа → `unknown` + низкая уверенность; (потом) веб-поиск с источниками; подтверждение пользователем |
| Оверлей посадки | строится из реальных чисел (замер + размер + ширина), **не генерируется**; помечен «приблизительно, 2D» |
| Фидбэк | детерминированные правила, без LLM в решении |
| Замер стопы | привязка к ориентиру (A4/размер); без него честно «недостоверно» |

---

## Сделано

### ✅ Шаг 1 — Модель → атрибуты (данные-фундамент)
Влито в `main`. Тесты: сервер 16/16, фронт 8/8, сборка чистая, живой прогон ОК.
По названию модели → атрибуты (бренд, материал, тянется, репутация по размеру и
ширине, носок) + уровень уверенности; правка/подтверждение; ошибка → ручной ввод.
Это данные, которые питают и оверлей (размер/ширина), и фидбэк.
Спека: [спека шага 1](superpowers/specs/2026-06-15-step1-model-attributes-design.md) ·
План: [план шага 1](superpowers/plans/2026-06-15-step1-model-attributes.md).
- _Заметки на шаг Anthropic:_ `getProvider()` сейчас на загрузке модуля (сделать
  ленивым для теста переключения); в провайдер уходит «сырое» имя модели (для
  веб-поиска это правильнее, чем нормализованное).

---

## Что делать

### ⬜ Шаг 2 — Ввод стопы (замер)
- Ручной ввод: длина, ширина, размер — отдельными полями, **с подписями против
  путаницы**: «длина стопы — от пятки до самого длинного пальца, в мм; это НЕ длина
  стельки и НЕ размер»; «ширина — самое широкое место».
- Фото с A4 как референс масштаба (для замера и будущего реалистичного фото).
- Логика ориентира: есть A4/размер → идём дальше; нет → крупное «недостоверно».
- Нужно и для оверлея, и для фидбэка.
- **Готово, когда:** замер хранится в мм + категория ширины, переводится между
  системами размеров; работает «ориентир или предупреждение».

### ⬜ Шаг 3 — Рисунок: рентген-оверлей посадки ⭐ (ядро визуала)
- Силуэт стопы (из замера) полупрозрачный + контур внутреннего пространства модели
  (длина из размера, ширина из репутации); зоны «тесно / впору / свободно».
- SVG/canvas, **без генеративного AI**, без оплаты.
- Пометки: приблизительно; 2D (без подъёма).
- **Готово, когда:** на экране видно силуэт стопы относительно контура модели с
  подсветкой тесных зон.

### ⬜ Шаг 4 — Фидбэк (словами)
- Подходит / тесно / свободно + причина + уровень уверенности (правила; LLM — текст).
- Идёт **после** рисунка.
- **Готово, когда:** на (стопа + модель) стабильный вердикт из 3 категорий + причина,
  с учётом наличия/отсутствия ориентира.

### ⬜ Мелочь — убрать «подбор» из интерфейса
- `src/App.tsx` + `index.html`: заголовок → например «Примерка: как сидят кроссовки».

### ⏸ Шаг 5 — Реалистичное фото «как выглядит на ноге» (опция, потом)
- Генеративное совмещение (фото ноги + кроссовок). Про посадку недостоверно и платно
  → строго опция с пометкой «примерный вид». AR/3D try-on не делаем (неподъёмно соло).

### ⏸ Шаг 6 — База данных (потом)
- Кеш проверенных моделей; затем каталог для рекомендаций.

---

## Граница MVP

**MVP = примерка:** `модель (✅) → замер стопы → рентген-оверлей → фидбэк` (шаги 1–4).
Привязка к ориентиру и дисклеймеры — часть MVP.

**Потом:** реалистичное фото (5), база/каталог (6), аккаунты, форма стопы (подъём,
косточки), монетизация.

---

## Стек

Vite + React (фронт) + Node-сервер `node:http` (прячет ключ). Оверлей — SVG/canvas
на фронте, без AI. Атрибуты — через провайдера: mock → Claude API + веб-поиск.
Фидбэк — обычный код (правила). Хранилища на MVP нет (порт сервера 8788).

---

## Текущий статус

- **Сделано:** решения зафиксированы; продукт = **примерка (рентген-оверлей → фидбэк)**;
  **шаг 1 (модель → атрибуты) реализован, отревьюен, влит в `main`.**
- **Не запушено:** работа в локальном `main`; пуш на `origin` — по желанию.
- **Следующее:** шаг 2 — ввод стопы (A4/ручной замер с подписями), затем шаг 3 (оверлей).
